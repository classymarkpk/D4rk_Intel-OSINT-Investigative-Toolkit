"""Lightweight utility helpers for repository scaffolding."""

def log(msg: str) -> None:
    """Simple logger used by CLI stubs.

    Keep this intentionally tiny so it's easy to import from other scripts.
    """
    print(f"[osint-utils] {msg}")
